node_modules/
.DS_Store

# Next.js (mantemos o build compilado para deploy no servidor)
# frontend/.next/  <- INCLUÍDO INTENCIONALMENTE
frontend/out/

# Backend (mantemos o dist/ compilado para deploy no servidor)
# backend/dist/  <- INCLUÍDO INTENCIONALMENTE
# Databases
*.db
*.db-journal
backend/prisma/*.db
backend/prisma/*.db-journal
backend/uploads/

# Environment variables
.env
.env.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
